#include <iostream>
using namspace std;
int main() {



/1. s/2. um_of_integers(strings):
    total = 0
    for s in strings:
        total += int(s)   # raises ValueError if s is not a valid integer
    return total
def greet(name):   # 'name' is a parameter
    print(f"Hello, {name}")

greet("Alice")     # "Alice" is an argument
def greet(name):   # 'name' is a parameter
    print(f"Hello, {name}")

greet("Alice")     # "Alice" is an argument
/3. # for loop
for i in range(5):
    print(i)

# while loop
i = 0
while i < 5:
    print(i)
    i += 1
/4. def factorial(n):
    if n <= 1:
        return 1
    return n * factorial(n - 1)
/5. def average_of_integers(strings):
    if not strings:
        return 0
    total = sum(int(s) for s in strings)
    return total / len(strings)
/6. def longest_string(strings):
    if not strings:
        return None
    longest = strings[0]
    for s in strings:
        if len(s) > len(longest):
            longest = s
    return longest
/7. def count_vowels(strings):
    vowels = set('aeiouAEIOU')
    count = 0
    for s in strings:
        for ch in s:
            if ch in vowels:
                count += 1
    return count
/8. def sum_vowels(strings):
    # Same as count_vowels – returns total number of vowel occurrences
    vowels = set('aeiouAEIOU')
    total = 0
    for s in strings:
        total += sum(1 for ch in s if ch in vowels)
    return total
/9. def remove_largest_vowel_and_sum(s="xandy"):
    vowels = set('aeiouAEIOU')
    
    # Find the largest vowel in the string (by character code)
    largest = None
    for ch in s:
        if ch in vowels:
            if largest is None or ch > largest:
                largest = ch
    
    if largest is None:          # no vowels at all
        return 0
    
    # Remove the first occurrence of that largest vowel
    new_s = s.replace(largest, '', 1)
    
    # Count vowels in the new string
    return sum(1 for ch in new_s if ch in vowels)

# Example:
print(remove_largest_vowel_and_sum("xandy"))  # Output: 0
/10. def remove_largest_vowel_and_sum(s="xandy"):
    vowels = set('aeiouAEIOU')
    
    # Find the largest vowel in the string (by character code)
    largest = None
    for ch in s:
        if ch in vowels:
            if largest is None or ch > largest:
                largest = ch
    
    if largest is None:          # no vowels at all
        return 0
    
    # Remove the first occurrence of that largest vowel
    new_s = s.replace(largest, '', 1)
    
    # Count vowels in the new string
    return sum(1 for ch in new_s if ch in vowels)

# Example:
print(remove_largest_vowel_and_sum("xandy"))  # Output: 0
